# ── 评论业务质检看板 - 依赖清单 ──
# 仅声明顶层依赖，间接依赖由 pip 自动解析

# 前端框架（含 altair, pandas, numpy, pillow, pyarrow, plotly 等子依赖）
streamlit>=1.55.0,<2.0

# 数据处理
pandas>=2.1
numpy>=2.0
openpyxl>=3.1            # Excel 读写

# 图表
plotly>=6.0

# 数据库
mysql-connector-python>=9.0   # TiDB (MySQL 兼容) 连接

# HTTP（企微 Webhook 推送 + DeepSeek API 调用）
requests>=2.31

# 配置文件解析
pyyaml>=6.0               # alert_modules.yaml 解析

# 测试（开发环境）
# pip install pytest pytest-cov
